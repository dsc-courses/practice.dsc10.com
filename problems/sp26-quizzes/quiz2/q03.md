# BEGIN PROB

Fill in the blanks to add a new column `"unlock"` containing strings of
the form `"Arena k"` where `k` is the corresponding value in the
`"arena_unlocked"` column (e.g. `3` $\rightarrow$ `"Arena 3"`).

    def make_unlock(arena_num):
        return __(a)__
    cards = cards.assign(unlock=__(b)__)


# BEGIN SOLUTION

`(a)`: `"Arena" + str(arena_num)` \

`(b)`: `cards.get("arena_unlocked").apply(make_unlock)`

<average>67</average>

# END SOLUTION

# END PROB