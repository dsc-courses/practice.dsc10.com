# BEGIN PROB

\[(10 pts)\]

Which of the following expressions evaluate to the name of the school
with the widest height range for its mascot costume (or any such school,
in the case of a tie)? **Select all that apply.**

**Hint:** `.groupby` orders rows in ascending order of the values in the
index.

[ ] `mascots.set_index("school").get("range").max()`
[ ] `mascots.set_index("school").sort_values(by="range").index[-1]`
[ ] `mascots.groupby("range").max().get("school").iloc[-1]`
[ ] `mascots.groupby("school").max().index[-1]`
[ ] None of the above.

# BEGIN SOLUTION

**Answer**: Options 2, 3, and 5.

<average>71</average>

# END SOLUTION

# END PROB