---
lecture: 4
---

# BEGIN PROB

For each expression below, write the **output** that Python produces, or write **error** if the expression produces an error.

# BEGIN SUBPROB

```py
str(courses.take(np.arange(4)).shape[0]) * 2
```

# BEGIN SOLUTION

**Answer:** `"44"`

`courses.take(np.arange(4))` keeps the first four rows, so `.shape[0]` is `4`. Then `str(4)` is `"4"`, and `"4" * 2` repeats the string twice → `"44"`.

<average>58</average>

# END SOLUTION

# END SUBPROB

# BEGIN SUBPROB

```py
courses.get("Enrollment").iloc[0] != courses.get("Enrollment").loc[0]
```

# BEGIN SOLUTION

**Answer:** `False`

`.iloc[0]` and `.loc[0]` both refer to the first row (label `0`), so they read the same enrollment value. Comparing a value to itself with `!=` is always `False` (e.g. `120 != 120`).

<average>40</average>

# END SOLUTION

# END SUBPROB

# BEGIN SUBPROB

```py
courses.shape[1] / 7
```

# BEGIN SOLUTION

**Answer:** `1.0`

`courses.shape[1]` is the number of columns, which is `7` for this DataFrame. So `7 / 7` is `1.0` (division in Python 3 returns a float; `1.` is also acceptable).

<average>78</average>

# END SOLUTION

# END SUBPROB

# END PROB
